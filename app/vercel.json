{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "yarn install",
        "buildCommand": "yarn build",
        "devCommand": "yarn dev"
      }
    }
  ]
}